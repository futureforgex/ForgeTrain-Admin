version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - if [ -f ./amplifyconfiguration.json ]; then mv ./amplifyconfiguration.json ./src/amplifyconfiguration.json; fi
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 